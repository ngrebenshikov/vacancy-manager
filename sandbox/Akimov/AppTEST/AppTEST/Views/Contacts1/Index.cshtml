@model IEnumerable<AppTEST.Models.Contacts1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Name
        </th>
        <th>
            profileID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.profileID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Contacts1Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Contacts1Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Contacts1Id })
        </td>
    </tr>
}

</table>
